# Handover — full-debug + slim-down pass (2026-09-24)

Session: https://claude.ai/code/session_01LMwXJ9BNMoMdeqcMnQhdCd (paused by the
user mid-way; the rest continues in another session). Delete this file once the
work below has been picked up.

## State of the tree at hand-over

- Baseline on the untouched tree (commit 816f66f): clippy 0 warnings (default
  and `gui`); `cargo test --release` 1666 lib pass / 0 fail / 14 ignored, CLI 25,
  contract 2+2, GUI bin 214 pass; check_docs 27 PASS / 5 SKIP; audit_i18n clean.
- CLI smoke on synthetic PNGs (decode/apply/match/match --zoned/stack ×5/heal)
  all exit 0; SHA-256 of every output recorded in the session scratchpad (not
  in the repo). Re-derive if needed with the same synthetic inputs.
- Whole-workspace dead-code compile (library + both binaries folded into one
  crate, every test as a root) found only 15 unreferenced items; all removed
  (see git log). No `#[allow(dead_code)]` hides anything else.
- **The final tree was checked with `cargo clippy --release --all-targets`
  only. The full test battery has NOT been re-run after the edits below** —
  the user hit the usage limit. First thing to do:
  `cargo test --release && cargo test --release --features gui --bin autoshade-gui`,
  plus `python3 scripts/check_docs.py` and `python3 scripts/audit_i18n.py`.
- Two audit sub-agents (scripts/*.py and site/workflows/installer) were
  stopped before reporting; their in-flight edits were reverted. Those two
  groups are unaudited.

## Behaviour-affecting fixes applied (each is a bug fix; watch these if a test moves)

- `src/fit_zoned/freemask/attach.rs` push_refusal: `FreeMaskWhy::Shrunk` was
  missing from the disclosure array, so the R39 shrunk refusal was never
  written to the ledger.
- `src/dcp.rs` table(): dims product via checked_mul (crafted .dcp could wrap
  to 0 and index an empty table → panic mid-render); Table::at in usize.
- `src/render/perspective/upright.rs`: mode 3 (Vertical) with no vertical
  vanishing point now refuses (`(3, None, _) => None`) instead of falling
  through to a horizontal keystone.
- `src/pipeline.rs` migrate_raster_file: staging name is per-call
  (`part-<pid>-<seq>`), not per-process; post-stamp domain note is computed
  AFTER carry_over_unrepresentable (it used to describe a lens profile the
  delivered recipe no longer had).
- `src/fit.rs` fit_cast_stage closure resets `cast_admission` per call (the
  strength-admission fact could describe a probe, not the shipped recipe).
- `src/render.rs`: resolve_camera_profile discloses a missing camera matrix
  via diag.warn instead of returning None silently; engine_active mirrors the
  render's NR gate (`> 0.1`, was `!= 0.0`).
- `src/serve.rs`: api_retouch / api_heal remove the staged mask temp file on
  early-return arms; read_json answers 400 (ClientErr) for a non-UTF-8 body
  instead of 500.
- `src/main.rs` correspond_cmd: `revision.get(..12)` (no panic on a short
  revision).
- `src/style.rs`: two user-facing strings had lost their `\` continuation
  (14–18 spaces mid-sentence) — restored; three rustdoc blocks moved onto the
  functions they describe (desc_text, consistent_mean, style_pull).
- Dead code removed: style::embed_staged / embed_preview,
  style_cache::cache_path, embed::embed_file (lib.rs:194 doc still names it;
  the "gate, not a batcher" rationale lived only in the deleted doc —
  consider re-homing onto embed_file_record), describe::cache_path +
  DescriptionCache::len, correspond FieldRecord.sim, decode
  CropAlignment::moved_to, diag Diag::sink/subject, retouch
  SpotOrigin::is_imported, pipeline::photo_base_knots_checked (dangling
  rustdoc links at pipeline.rs ~3080/3090 → retarget to
  photo_base_knots_checked_in), fit_zoned::fit_recipe_zoned_with
  (gui/actions.rs:3471 comment still names it), store::migrate_legacy_from
  (dangling link in migrate_legacy_from_many's doc) and
  EditStateKind::Variant.origin (its initialiser called read_pixel_source —
  only duplicate stderr disclosures are lost), a duplicate base_curve
  clamp pass in recipe.rs clamp(), a duplicate evaluate(lambda) in
  fit::solve_white_balance, `!stamp &&` tautology in gui/workers.rs, a
  cfg(test) strip_thumb_rect write (app.rs:388/1791 field now unused), a
  shadowing `let ext = ext.to_string()` in gui/export.rs, python
  `_KAIR_RELEASE`, `TEXT_SPECIAL_TOKENS`, `_nullctx` dedup (denoise_raw.py
  now imports it from denoise.py), unused imports/f-prefixes in scripts.

## Findings NOT yet fixed (from the per-file audit; verify before acting)

# Findings triage (from subagent reports)

## advisor/* (no edits made)
- B1 mod.rs:189-197 project_remote_recipe_text re-bounds returned mask names (ctrl chars stripped, >256B cut) but pipeline.rs:1634 refine matches m.name == original.name byte-exact → a base mask named with ctrl chars / >256B can never match → all mask edits discarded + MASKS_NOT_PRESERVED. medium-low / med-high. Fix idea: compare against the projected base name, or don't re-bound a returned name equal to a base name.
- B2 openai.rs:1009-1024 extract_output_text ignores `refusal` parts → reported as Transport shape mismatch. low/med.
- B3 judge.rs:172-180 zoned vocabulary includes "local"/"area" → "local contrast" hint triggers zoned re-solve. low/med.
- B4 judge.rs:167-170 SAT_DOWN has "too " and "lower" → "too weak" inverts. low/med.
- B5 mod.rs:701-712 into_text_capped read_to_string fails on non-UTF-8 → body "" → no param negotiation, empty error. low/med. Fix: from_utf8_lossy.
- B6 mod.rs:673-691 json_from_reader_capped take(cap) → over-cap 2xx body reported as unreadable JSON instead of naming the cap. low/high/minimal.
- B7 catalogue.rs:2986 `upright_transform: _` has no GlobalValue arm; doc at 2920-2928 & comment 2672-2675 claim otherwise. doc-only.
- B8 catalogue.rs:2315-2317 "20 families … ten engine-only" but CONTROL_FAMILIES has 21 / 11; mod.rs:464-466 "one per family (9)" but 10 AI-visible. doc-only.

## style.rs + style_cache.rs (edits: removed embed_staged, embed_preview, style_cache::cache_path; -34 lines)
- F1 HIGH style.rs:~3006 apply_cached / :3100 adopt_cached_desc_embed after the pool: digest-rescued cache entries copied WITHOUT entry.embedding_is_current(&provenance); :1855 cache_entry re-stamps old vector with new provenance → stale vectors laundered when MODEL_REVISION/tokenizer/vocab changes. Fix: gate on embedding_is_current(provenance) (or Bands::admits checks provenance as doc :33-40 claims).
- F2 MED style.rs:3282-3293 vocab mismatch drops looks but leaves RAW exemplars' vocab_scores/tags/desc_embed stale (only after a LOOK_VOCAB_VERSION bump; v1 today).
- F3 HIGH-mech style.rs:3175-3181 save merge keeps self.embed_provenance (None w/o --embed) → file rewritten with null stamp; load :3282 treats missing stamp as match, contradicting :578-580 doc.
- F4 MED-LOW style.rs:3126-3134 embed-off rebuild prunes cache entries of touched-but-unchanged files (no digest staged) contradicting cache_entry doc :1835-1841.
- F5 HIGH/low style.rs:2638-2670 build_looks_with leaks vocab scratch file on error paths (? at 2647/2648/2669 before remove at 2670).
- F6 HIGH/low style.rs:3137 and :3287 user-facing strings with 14-18 spaces (lost `\` continuation); docs show single-space form. Fix: restore `\`.
- F7 HIGH/trivial rustdoc attached to wrong item: desc_text doc on vocab_scratch_path (:1430-1445); consistent_mean doc on label_is_one_sided (:4201-4230); style_pull doc heads distillation_preview (:4848-4870).
- F8 HIGH/trivial DistanceTerms::txt_hub doc vs code (:2431 vs :3532/:3586).
- F9 LOW style_pull discontinuous at 0.5 (0.49→0.294, 0.50→0.500); tests pin 1.0→1.0, 0.3→0.18.
- F10 MED walkdir :769-779 follows dir symlinks → cycle loops forever.
- F11 HIGH/cosmetic attach_descriptions returns w/o closing report(total,total) when manifest write fails (:2107-2109).
- cross: pipeline.rs:323 and segment.rs:1338 comments mention removed style::embed_preview; style_cache.rs:53 doc link [`cache_path`] dangles (repoint to cache_path_in). embed.rs embed_file now callerless (G13 has it).
- kept: ExemplarCache::is_empty (clippy len_without_is_empty) — correct call.

## fit_zoned/* + fit_field + fit_cells (no edits)
- B1 HIGH attach.rs:19-26 push_refusal array omits FreeMaskWhy::Shrunk → R39 shrunk refusal (attach.rs:245) never disclosed; label "shrunk-below-acceptance" never emitted. Fix: add variant to array (+ test).
- B2 MED field.rs:473-495 zone_regressed only checks ZoneSky|ZoneLand → inert on multi-region (Custom role, name "region-…") route; doc promises every attached zone re-measured. Behaviour change if fixed (needs corpus measurement) → report only.
- B3 LOW/MED field.rs:393-406 shipped ColourField rounded after acceptance judged on unrounded → err_after describes unrounded render. tiny magnitude. report only.
- B4 LOW field.rs:360-363 silent abstention when terminal re-solve returns None (no note). report only.
- B5 LOW semantic.rs:111-131 resolve_regions sizes claimed_* from first candidate; later larger plane would panic; unreachable today. hardening only.
- kept: fit_field.rs:111 stale cfg_attr allow(dead_code) on LocalField::grid (now used in prod) — harmless.

## fit_zoned.rs (edit: removed fit_recipe_zoned_with, -21 lines)
- #1 doc/code: ZONE_TEXTURE_MAX=1.95 but rustdoc :100-102 says "2.05 ceiling … accepted maximum of 1.972"; :93-94 says accepted max 1.918; ROADMAP-archive pins [0.70,1.95]. doc-only fix (restate as 1.95 / 1.918).
- #2 MED attach_one_zone: zone_before measured with robust-composed weights zws, m_after with raw sw; AcceptedZone.source_weights stores raw not estimator population. Calibration-affecting → report only.
- #3 LOW tone ladder [0.75,0.5,0.25,0.0] never tries 1.0. calibration → report only.
- #4 LOW/MED :5213-5232 drop note prints zone_before/after where verdict used accepted_before/after. disclosure only.
- #5 cross: gui/actions.rs:3471 comment names removed fit_recipe_zoned_with.
- #6 :3194 comment cites nonexistent spatial::sweep_tiles (is attach_tiles).

## recipe.rs group (edit: removed duplicate base_curve retain/clamp pass, -8 lines)
- F1 MED-HIGH recipe.rs clamp() never sanitises colour_field; renderable() x*y*b unchecked mul; render.rs:2447-2477 indexes grid → panic from posted recipe (serve /api/develop) with huge x/y/b (wrapping→0==grid.len()). Fix: checked_mul in renderable + drop absurd fields in clamp, clamp amount.
- F2 MED clamp() contract not met for look/camera_profile/upright_transform (unbounded strings, uncapped curves). low sev.
- F3 HIGH rationale.rs:1322-1327 sanitize_error_paths drive-letter rule fires on "https://" → "httpcompletions". Fix: require non-alnum before drive letter.
- F4 HIGH/low payload.rs:176-179 rasters_element: shared raster failed embed → loss only for first mask.
- F5 HIGH/cosmetic payload.rs:353-366 take(MAX_RECIPE_JSON) truncation → CRC mismatch instead of "too large".
- F6 HIGH/rare payload.rs:962-966 claim_beside leaves partial file on write error.
- F7 MED/low rationale.rs:1299-1315 exit_code substring match "exit" inside words.
- F8 doc misattached: recipe.rs:3309-3311 clamp() doc on MAX_RATIONALE; openai_models.rs:7-8 list_models doc on MODELS_RESPONSE_CAP; rationale.rs:1203 "67 pointers" vs 68.
- openai_models.rs:215 stray paste marker in tests `// FILE: src/config.rs (append inside…)` + mis-indented test 197-213 (test module).

## pipeline group (edit: removed photo_base_knots_checked, -10 lines)
- F1 MED pipeline.rs:2428 migrate_raster_file staging `.part-{pid}` per-process not per-call → concurrent migrators (serve threads) can publish half-written raster. Fix: add crate::store::next_tmp_seq() like save_master/preflight_out.
- F2 MED-HIGH pipeline.rs:1337-1351 post_stamp_domain_shift note computed before carry_over_unrepresentable replaces lens_profile → note may say frame differs by lens profile while returned recipe applies none. Fix: move 2 lines below the carry-over block. (rationale sentence only)
- F3 doc pipeline.rs:2795-2797 photo_calibration claims produce_recipe single-snapshot; it takes two. doc.
- F4 HIGH/low-reach pipeline.rs:5366-5373 walk: visited.insert before depth>64 backstop → dir first seen at depth 65 recorded but never read.
- F5 MED/low pipeline.rs:2892-2897 post_stamp_domain_shift uses "*_on" switch instead of vignette_active()/geometry_active().
- dangling rustdoc links [`photo_base_knots_checked`] at pipeline.rs:3080/:3090 → retarget to _in; prose mentions diag.rs:52, decode.rs:2636/2654; sha256.rs:38 [`frame_digest`] unresolvable (pre-existing).
- misplaced rustdoc: pipeline.rs 2925-2935 (calibration_recipe doc on FIT_SOURCE_EDGE); 3641-3651 (guard_readonly doc on normalize_lexical).

## fit.rs (edit: removed duplicate evaluate(lambda) call, -1 line)
- F1 HIGH-dead/MED-HIGH-unintended fit.rs:3653 & :4184 terminal_harm(err_before, err_after, None, None) while joint_base/joint_after computed just above → JOINT veto arm unreachable; FIT_NOTE_JOINT_REGRESSED/EVIDENCE_CONTRADICTED unreachable. ARCHITECTURE:5624 says it's a veto. Behaviour-changing fix (fixtures may move) → needs corpus; report.
- F2 HIGH-mech fit.rs:3329 cast_admission sticky across probe runs (never cleared in fit_cast_stage closure) → stale FIT_NOTE_CAST_ADMITTED_BY_STRENGTH. Fix: reset at closure start. (disclosure only; safe?)
- F3 MED fit.rs:5497/5568/5575 rescore_report measures against EditRecipe::default() not calibration base.
- F4 MED fit.rs:4096-4099 / 4121-4129 Atmosphere path publishes stale WB facts and stale hue-veto disclosure.
- F5 doc fit.rs:536-537 structure_blind doc claim only holds trivially.
- F6 LOW-MED fit.rs:1498 manufactured correlation 1.0 when no offset yields sa*sb>0.
- F7 MED/low fit.rs:8558-8590 only_detail_and_quantized_companions ignores recipe.hsl.
- minor: :1470 literal 100 duplicates STRUCTURE_MIN_CORE_PX; :3923 "exactly two" WB-reset sites (three); :360 no-op assignment; :567 label prints unclamped.

## GUI actions/workers/canvas/masks (edit: workers.rs removed tautological `!stamp &&`, -1)
- F1 MED actions.rs:2598-2601 sync_base_turns: baked-card branch sets base_turns=want without turning plates; reachable via rotate → AI denoise (◈ card from EXIF-frame raster) → canvas shows un-turned picture under turned-frame geometry. Real cure: gate denoise/stack/clone/reimagine behind refuse_pixel_work_on_a_turned_photo (retouch.rs) or turn source plate. Report (GUI behaviour change).
- F2 LOW actions.rs:2602-2622 turn doesn't rebind mask_paint canvas dims. Fix: rebind_paint_canvas.
- F3 LOW canvas.rs:905-909 handle_range_pick reference recipe doesn't zero ca_r/ca_b unlike refresh_mask_overlay :225-229 → cache miss with manual CA. Fix: add 2 lines. (perf only)
- F4 doc misattached: canvas.rs:6-18 canvas_edge doc on apply_zoom_glide; canvas.rs:1026-1039 handle_region_select/handle_mask_edit docs on sel_target_geometry.
- F5 stale prose: workers.rs:414-416, actions.rs:1029-1031, canvas.rs:875-877, canvas.rs:843.

## decode/lcp/lensmeta/dcp/adobe/config (edit: decode.rs removed CropAlignment::moved_to impl, -10)
- F1 HIGH-mech dcp.rs:393 want = d0*d1*d2*3 unchecked; Field::floats returns empty for unmodelled TIFF type → crafted .dcp accepted with empty data, lookup panics mid-render. Fix: checked_mul chain → TableMismatch. (local profile pool = untrusted per lcp.rs:843)
- F2 MED/low decode.rs:1968-1973 doc says decoder_for is the ONE door but render.rs:270 and decode.rs:2285 call rawler::get_decoder directly (older wording, no A7/A8 remedies). Fix: route through decoder_for.
- F3 doc config.rs:1150-1161 effort() doc glued onto same_endpoint.
- F4 MED/low config.rs:422-432 non-UTF-8 settings file skipped, next save overwrites (unlike corrupt-JSON rescue). Fix: preserve_corrupt_settings on InvalidData.
- F5 MED/low decode.rs:688-693 TIFF ICC re-probe drops profile >1MiB silently (tiff limits → Ok(None)).
- F6 LOW decode.rs:1462-1492 source_window baked arm bypasses baked_reader.
- kept: decode.rs:909-911 unreachable MAX_BAKED_EXIF check (JPEG u16 length) — belt and braces w/ string literal.

## retouch/mask_refine/mask_habit/diag/denoise/stack (edits: diag.rs sink()/subject() -8; retouch.rs is_imported -5)
- F1 MED retouch.rs:463-514/533-535 find_donor returns sentinel (0,0) when no fully-in-bounds donor ring → heal_one silently no-op but counted as healed ("1 spot healed", pixels unchanged) for blobs ≥~450px radius on 4000×3000. Fix: Option return + count/disclose. Behaviour change (disclosure) → consider.
- F2 perf align.rs:172-186 Warp::apply rebuilds Vec per call (48M allocs on 24MP). Fix: split planes once. Output identical.
- F3 denoise.rs:429/435/441 `let _ = enc.set_icc_profile` swallows refusal (inert today).
- F4 doc mask_habit.rs:480-481 "other seven ±100" → ten. F5 doc mask_refine.rs:391-393 max_radius doc drift.

## python sidecars (edits: denoise_raw.py -9 [_KAIR_RELEASE, _nullctx dedup → import from denoise], embed.py -7 [TEXT_SPECIAL_TOKENS])
- B1 MED denoise.py:276-306 _fetch_verified: stale cached file consumes the FIRST source (mirror) without downloading; mismatch blamed on mirror. Fix: hoist existing-file check above loop (moves text pinned by src/denoise.rs "_reclaim_stale_parts(dest)\n    tried = []\n    for source in").
- B2 MED-LOW segment.py:1005-1006 write_multi_manifest drops prefer_cpu (--multi --cpu ignored); Rust never passes --cpu here. Fix: pass prefer_cpu.
- B3 LOW denoise_raw.py:866-868 failed cv2.imwrite leaves .part behind (denoise.py wraps in try/finally). Fix: same try/finally.

## render.rs (no edits)
- B1 HIGH-mismatch/low-reach render.rs:1706 resolve_camera_profile returns None silently (no diag.warn) when camera_matrix empty after dcp::find succeeded; doc promises disclosure. Fix: warn then return None.
- B2 MED render.rs:5033-5043 inverted alpha-less Brush/AiMask component → whole-frame coverage (Add) / wipe (Subtract). low reach. design call.
- B3 HIGH/trivial render.rs:4654 engine_active uses != 0.0 for noise_reduction but render gate is > 0.1 (:3148). Fix: mirror.
- B4 doc misattached: 7227-7278 camera_base_knots doc on camera_frame_of; 9111-9115 sample_bilinear_rgb16 doc on sample_bilinear_ch.
- B5 doc: 6588 [`apply_vignette`] broken link; stale line citations 6152, 6280, 9834.

## render/* (no edits)
- B1 HIGH upright.rs:259-268 mode 3 (Vertical) with pv=None, ph=Some falls through to third arm → horizontal keystone applied. Fix: `(3, None, _) => None,` + test.
- B2 MED-HIGH upright.rs:251-254 levelling rotation in normalised coords → shear on non-square frames (same defect fixed for manual Rotate at perspective.rs:292-300). Fix: φ_p = atan2(sin φ_n, aspect·cos φ_n); about_centre([c, -s/aspect, 0, s*aspect, c, 0, ...]). Behaviour change on real photos → evaluate.
- B3 MED upright.rs:259-271 keystone row from unrotated VPs applied after rotate → residual convergence 1−cosθ. second-order.
- B4 MED/low profile.rs:283-295 sample lower branch always ±0; upper extrapolates chord not tangent.
- B5 MED-HIGH/tiny finish.rs:478-486 vignette sampled at pixel corners vs centre w/2 → ½ px offset.
- B6 LOW upright.rs:118 lo < 0.0 refuses on rounding noise.
- B7 MED perspective.rs:274-281 perspective_scale=0 → singular map swallowed (unwarped render silently). Fix: floor s.
- B8 doc perspective.rs:178 `then` doc reversed; :336 param name.
- B9 test-only lr_pack.rs codes() accepts .XMP but reopens lowercase.

## serve.rs / main.rs / web (no edits)
- #1 doc main.rs:1761-1778 denoise_cmd rustdoc sits on stack_cmd; fn denoise_cmd (1837) undocumented. Move.
- #2 HIGH/low serve.rs:2860-2880 api_retouch & 2970-3009 api_heal: staged mask temp file leaks on early-return arms (unique_out None → 500; session_master Err). Fix: remove_file in those arms.
- #3 HIGH/low serve.rs:3435-3465 read_json read_to_string InvalidData (non-UTF-8 body / over-cap cut mid-char) → 500 instead of 400. Fix: map InvalidData → 400.
- #4 MED main.rs:759 &field.revision[..12] panics on short/non-ASCII revision. Fix: .get(..12).unwrap_or.
- #5 doc serve.rs:483-484 SAVE_LOCK doesn't exist (means develop lock).
- #6 index.html:1634-1638 dead branch on phrase server never sends. optional.
- #7 LOW serve.rs ~1015-1110 corrupt central recipe + valid legacy served w/o disclosure.
- #8 test doc split main.rs:3358-3372/3438.

## store.rs (edits: migrate_legacy_from -9; EditStateKind::Variant.origin field + initialisers -9 (NOTE: drops duplicate stderr disclosures from read_pixel_source in list_edits); `let _ = retired;` -1)
- B1 MED store.rs:6039-6079 migrate_legacy_from_many (gallery import) doesn't run recover_orphan_baks first → older legacy recipe published over newer .bak survivor. Fix: call recover_orphan_baks_unlocked(p) in closure at 6073 like 5990.
- B2 MED-HIGH/low store.rs:5459-5471 delete_version of nonexistent n writes durable marker before NotFound → next recovery burns n and hwm. Fix: probe version_target before marker or remove marker on NotFound.
- B3 MED/low store.rs:94/139-150 Wait-mode lock gets hard error when fenced adoption in flight (resume_marked_adoption uses NoWait).
- B4 MED/low store.rs:2552/2623-2660 variants_member drops named pristine card identity when pixels.json missing.
- B5 doc store.rs:6213/6249/6279-6285/6309 stale deletion prose; empty `if published {}` block; dangling [`migrate_legacy_from`] link at 6034.

## xmp.rs (no edits)
- F1 HIGH xmp.rs:5980-5994 unparsable_crs_numbers applies l<r && t<b unconditionally; reader accepts Left>Right when CropAngle≠0 → false "inconsistent crop" disclosure. Fix: `(l < r && t < b) || angle != 0.0`.
- F2 MED-HIGH xmp.rs:1093 crop_import_note overshoot px uses wrong axis/frame (fractions of wi/hi × f.w.max(f.h)). sentence number only.
- F3 HIGH/low-freq xmp.rs:2109/2124/6357/6366 mask-import disclosures not gated by xmlns_conflict → reports masks skipped with wrong reason. Fix: add `|| xmlns_conflict(xmp).is_some()`.
- F4 MED xmp.rs:5249-5304 vs 8876-8890 orientation-only sidecars: writer folds Angle through photo aspect, reader w/o payload can't unfold → disclosure/recipe disagree. maybe known limitation.
- F5 doc misattached: 2779-2786 masks_xml doc on written_name; 3057-3061 owned_attrs doc on amount_carries; 4372-4375 find_matching_close doc above CONSTRUCTS.
- F6 test side effect 10073-10076 writes out/_masks_test.xmp into cwd on every cargo test.
- F7 HIGH/no-writer xmp.rs:2463-2467 unmodelled_global_crs skips only ASCII spaces before `=`.
- flagged: 8548 duplicate correction_mask_components(seg)? call.

## GUI panels + export (edits: develop.rs removed cfg(test) strip_thumb_rect write -4; export.rs removed shadowing `let ext = ext.to_string()` -1)
- F1 HIGH-mech retouch.rs:637-647 start_clone lacks refuse_pixel_work_on_a_turned_photo gate (fill/adjust/heal have it) → clone on turned photo lands transposed and bakes in place. Fix: add the gate at :640.
- F2 MED settings.rs:149-168 refused-key detection reads resolved presence (env can mask a refused file key) → "saved" although typed key rejected. Fix: check the FILE's key.
- F3 LOW export.rs:124-131 vs persist.rs:159-161 import-loss count taken before clamp (>64 masks).
- F4 doc ai.rs:61-70 ai_panel doc on ai_section_active. F5 ai.rs:93 stale comment. F6 develop.rs:1759 broken link crate::xmp → autoshade::xmp.
- app.rs:388/:1791 strip_thumb_rect cfg(test) field now never written/read → drop.

## GUI core (edit: app.rs removed orphaned heading comment -2)
- B1 MED persist.rs:256-262 unreadable central XMP → NoopOnly instead of Unreadable → backup gate lost, next save overwrites. Fix: mirror recipe loop's parse_err.
- B2 MED-LOW persist.rs:776-820 strip_from_record never clamp()s restored variant recipes → malformed crop panics in upd_shortcuts (app.rs:822). Fix: v.recipe.clamp() per card.
- B3 LOW app.rs:1547 Shortcuts window lacks fixed .id → state lost on language switch (Settings window has .id).
- B4 doc model.rs:1450-1458 CROP_ASPECTS doc attached to EXPORT_SPACES. B5 main.rs:68 misplaced doc line (app_icon). B6 app.rs:349 names removed step_zoom_glide.

## eval/generative/segment/correspond/describe/embed (edits: correspond.rs field sim -3; describe.rs cache_path + len -9 (link retargeted to cache_path_in); embed.rs embed_file -33 — NOTE lib.rs:194 doc still names embed::embed_file; the "gate not batcher" rationale was in the deleted doc)
- #1 HIGH generative.rs:805-829 flex_size floors edges independently → ratio can exceed 3:1 (42% of budgets for ≥3:1 sources) → API 400 + wasted round trip. Fix: floor short edge first, derive long.
- #2 HIGH describe.rs:278-300 sanitize_desc off-by-one at cap (513 chars w/ trailing space) → exemplar refused as "non-finite". Fix: check used+pending before push.
- #3 HIGH/low describe.rs:509-549 retain water-fill under-fills cap when own group largest. Fix: sort by (len, own-root tiebreak).
- #4 HIGH/latent correspond.rs:157-182 fit_provider pid-only temp names → two fits in one process collide. Fix: add seq.
- #5 correspond.rs parse_field accepts any revision (main.rs slice fixed by coordinator).
- #6 doc generative.rs:1511-1535 read_sse_image doc split across streamed_refusal_blames/LEAD_SNIFF_LIMIT. #7 doc generative.rs:118-121 stale invariant. #8 LOW scratch files survive parse failure (describe.rs:189-203, embed.rs:180-191/304-321/393-415). #9 embed.rs:470 message. #10 describe.rs invisible set doc/threat gap.
- kept: segment.rs:133-137 dead is_finite scan over u8 (documented bridge guard); MultiClassPlane.path/.share write-only.
