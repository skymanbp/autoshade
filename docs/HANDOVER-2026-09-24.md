# Handover — full-debug + slim-down pass, second session (2026-09-24)

Sessions: https://claude.ai/code/session_01LMwXJ9BNMoMdeqcMnQhdCd (first half,
the audit) and https://claude.ai/code/session_01BkfZN5PHFLe58VMfzbzUkg (this
one, the fixes). Delete this file once the items below have been picked up or
ruled on.

## Verification state of the tree at hand-over

- Before any edit (commit 56942e2): `cargo test --release` 1666 lib pass / 0
  fail / 14 ignored, CLI 25, contract 2+2; GUI bin 214 pass / 1 ignored;
  check_docs 27 PASS / 5 SKIP; audit_i18n clean; CI on main green for the
  first session's three commits.
- On this tree: clippy 0 warnings on both feature sets; check_docs 27 PASS /
  0 FAIL / 5 SKIP; audit_i18n every counter 0; python sidecar suites 73 passed
  / 3 skipped (torch absent); CLI smoke on synthetic PNGs (decode, apply,
  Upright 1–5, match, match --zoned, stack ×5, heal ×2, apply of the fitted
  recipes — 19 outputs): byte-identical to the pre-fix binary except the
  Upright LEVEL render, the one path whose geometry this session corrected.
- The release battery (`cargo test --release`, then the GUI bin) was running
  when this file was committed; the follow-up commit records its numbers.
  1735 library tests exist now (55 new).

## What this session changed (the short form; the commit message has the long one)

- Every finding of the first session's audit was re-verified by twelve fix
  agents, one per file group, each editing only its own files; the coordinator
  reviewed every diff. Fixes carry a unit test that fails before the fix.
- Behaviour changes to watch on real photographs (each is a corrected contract,
  not a re-tuning): Upright levelling on non-square frames is now a rigid pixel
  rotation (was rotated inside the normalised box → shear + wrong angle); a
  heal spot too large for any donor is left untouched AND counted as skipped
  (was silently counted as healed); the four mask-free pixel workers (AI
  denoise, stack, clone, reimagine) refuse a turned photo like heal/fill/adjust
  already did; a style-index build without the embedding pass no longer
  rewrites the exemplar cache; the recipe `clamp()` bounds the camera profile,
  the creative Look and the colour field like every other field; a refine
  reply's mask name that echoes the base is no longer re-bounded (so mask
  edits survive); the OpenAI `refusal` part is a ModelFailure, not a transport
  shape mismatch; `flex_size` never exceeds the API's 3:1.
- Two groups the first session left unaudited were audited: `scripts/` (fixed
  in place) and `site/` + workflows + installer (read-only; fixes applied by
  the coordinator where safe — see the commit).

## Findings NOT fixed (verify before acting; each has a proposed patch)

## advisor
- B3 judge.rs hint_action: zoned vocabulary has "local"/"area" → "add local contrast" buys a zoned re-solve (discarded unless it re-scores ≥; cost one zoned solve + judge call). Patch: `&& !h.contains("local contrast")` or drop the two words. Confidence medium; calibration on model text.
- B4 judge.rs SAT_DOWN has "too " → "too weak/flat" inverts to a pull-back. Pinned: "the render is too saturated" → down; "lower the saturation" → down. Patch: replace "too " by over-reach phrases (too much/strong/heavy/intense/far/saturat/vivid/colourful/colorful/punch). Confidence medium.
- openai_verify.rs verify: chat-completions `choices[0].message.refusal` not read → lands in the "no content" ModelFailure (correct kind). Cosmetic.

## style.rs
- F9 style_pull discontinuous at 0.5 (0.49→0.294, 0.50→0.500). Docs pin only 0.3→0.18 and 1.0→1.0. Patch through both points: `if s <= 0.3 { s*0.6 } else { 0.18 + (s-0.3)*(0.82/0.7) }` moves every Style in (0.3,1) → render-affecting; design call.
- save(): one embed_provenance stamp for two populations (a looks build stamped p merging over RAW vectors stamped q≠p keeps p). Needs a per-population stamp or a disclosure.
- load(): RAW exemplars' tags not bounded at the door like looks' (≤LOOK_TAGS_K, ≤128 chars); retag overwrites when vocab_scores present. Low.
- build_looks_with: a dangling symlink in the look folder is `!is_dir()` → decode_any fails → the whole look build fails. Pre-existing.

## recipe group
- upright_transform COUNT unbounded in clamp (only indices 0..=5 reachable via perspective_upright). Patch: `self.upright_transform.truncate(6)` after the finiteness check. Confidence high; memory-only.
- rationale.rs sanitize_error_paths: the unix rule still collapses "https://host/v1/chat/completions" → "https:completions" (`//` after `:`). Design call whether URLs should pass; they may embed credentials, so collapsing is the safe default.
- payload.rs rasters_element: one mask naming the same failing raster through two geometries records two identical RasterNotEmbedded entries. Cosmetic.
- recipe.rs clamp: non-finite look.amount → 0.0 (Look inert) while xmp read_creative_look maps an ABSENT amount to 1.0. Consistent with clamp's corrupt→inert rule; the two doors disagree on "unknown". Design call.

## xmp.rs
- F4 orientation-only sidecars (tiff:Orientation, no dims; 154/174 library sidecars): merge_frame folds corners/Angle through the photo aspect while the document declares no rectangle; a document-only re-read (no payload) reads folded corners as unfolded and discloses a FALSE "rotation not imported" for a rotated radial. With the payload present (every merge writes one) restore is exact. Reader-side patch: in xmp_to_recipe_clamped_impl after `honour_declared_orientation`, `frame = frame.or_else(|| source.and_then(|((w,h),_)| FrameAspect::from_size_turned(w as f64, h as f64, turn.turn)))` passed to read_crop and parse_masks_with_source, plus `_for_photo` twins of crop_import_note/import_losses. Writer-side alternative: have merge_frame's Keep/Orientation arms declare tiff:ImageWidth/ImageLength. Both move import behaviour on real sidecars → corpus check.
- crop_import_note: the `None => "% of one edge"` arm is unreachable (a Read with overshoot>0 implies a frame). Dead code.
- main.rs lightroom_import_note prints describe_import_losses + crop_import_note but never unparsable_crs_numbers (pipeline.rs ~3449 may) — unverified whether a namespace conflict surfaces on that CLI path.

## eval/generative/describe
- correspond.rs parse_field accepts any model/revision string from disk (printed at main.rs). Optional hygiene: refuse newline / >128 chars.
- describe.rs is_invisible: Unicode TAG block U+E0001, U+E0020–E007F (invisible-ASCII prompt-smuggling carrier), U+061C, U+180E are Cf and unstripped. If adopted: add arms to is_invisible AND the same ranges to python/describe.py's regex (or switch Python to unicodedata category Cf). Confidence high it is a real vector.

## GUI
- masks.rs start_mask_refine (433-517): the full-res guide goes through render::source_pixels (EXIF frame) while the raster it refines lives in the turned plate frame; no quarter_turns handling and no refuse_pixel_work_on_a_turned_photo gate — same frame-mismatch class as the four doors gated this session. Medium confidence; verify before gating.
- actions.rs sync_base_turns baked-branch comment presumes a master can carry the turn; the branch's real justification is the untracked per-variant v.base frame. Reword now that the four doors are gated.
- panels/settings.rs save_settings_form: with SettingsOrigin::SharedRoot, Config::load strips the just-saved file keys as untrusted (env only) while the GUI says "saved → path" (stderr warning invisible in the windowed GUI). UX call.
- persist.rs strip_from_record: clamp losses on strip cards are undisclosed (no channel to the toast). Design call.

## pipeline / fit / fit_zoned
- fit.rs F1 JOINT veto unreachable: both sites call terminal_harm(err_before, err_after, None, None) although joint_base/joint_after are computed just above; FIT_NOTE_JOINT_REGRESSED / EVIDENCE_CONTRADICTED unreachable in production. ARCHITECTURE ~5624 and terminal_harm's doc describe the arm as live. Patch (both sites): `terminal_harm(err_before, err_after, joint_base, joint_after)`. Behaviour-changing (joint drift > JOINT_DRIFT_TOL resets to base); joint_family_is_calibrated_on_the_fixture_set + SAME_CONTENT_DIAG cornwall test read these numbers → battery + corpus.
- fit.rs F3 rescore_report measures against EditRecipe::default() on the neutral fit source, not the calibration base. Patch: add `base: &EditRecipe`; callers main.rs:2073 / gui actions.rs:3706 pass &fit_base; fit_zoned.rs:9048 + ~9 tests pass the default. Moves rescored err_before/mode for non-neutral calibrations only.
- fit.rs F4(b) Atmosphere veto_hue disclosure reads moved_hue before the detail/HSL/shrink/terminal stages. Patch: `veto_hue: (budget.vetoes == VetoPolicy::Disclose && structural.global_cast.is_none()).then(|| moved_unsupported_hue_range_names(sp, &after_px, veto_evidence)).flatten()` (mirrors veto_luma). Risk: FIT_NOTE_VETO_DISCLOSED(hue) + the ≥0.85 confidence cap on Atmosphere fixtures.
- fit.rs F6 structure_divergence manufactures correlation 1.0 when no offset has sa*sb>0 (flat gradient). Patch: `if best <= -1.0 { return None; }` (abstain). Flat synthetic fixtures would read None → battery.
- fit.rs F7 only_detail_and_quantized_companions ignores recipe.hsl (narrow window err_after ∈ (err_before+1e-6, +1e-4]). Patch: `&& recipe.hsl == base.hsl`. Corpus.
- fit_zoned #2 attach_one_zone: zone_before on robust-composed zws, m_after on raw sw; AcceptedZone.source_weights stores raw. Patch: zone_moments(&zoned_px, &zw_source) and store zw_source. Every zone acceptance number moves.
- fit_zoned #3 tone ladder [0.75,0.5,0.25,0.0] never probes 1.0 → every luma-only zone ships ≤0.75 of its fitted tone. Patch: prepend 1.0. Calibration.
- fit_zoned/field.rs B2 zone_regressed filters ZoneSky|ZoneLand → inert for Custom-role `region-*` masks. Patch: judge every raster-backed mask this fit attached. Corpus.
- field.rs B3 err_with/zone_regressed measured on the unrounded field; round() after acceptance. Tiny.
- field.rs B4 silent abstention when LocalField::solve returns None (and solve_local_field). No existing key; proposed FIELD_UNSOLVED (rationale key + EN sentence + both GUI languages).

## CI / packaging / site (read-only audit)
- release.yml: `choco install innosetup -y` unpinned (installer_scenarios asserts Inno 6.7 wording); actions by mutable tag (dtolnay/rust-toolchain@stable, Swatinem/rust-cache@v2) in a contents:write workflow → pin to SHAs; `pip install torch torchvision` unpinned in macos-battery which `publish` needs (mps_probe returns 0 unconditionally → split it out of publish.needs or pin); `gh release create … || gh release upload --clobber` falls back on ANY create failure and a workflow_dispatch from a branch tags that branch head → `gh release view` first + refuse when GITHUB_REF_TYPE != tag; the 7-line checksums.txt was replaced by hand on v1.6.0 (uploader 17:36:15Z, .pth row added) while notes.md says "reproducible from this tag" → append the pinned .pth line in the checksum step or say so in the notes.
- installer/autoshade.iss: [Files] ships LICENSE but not README.md (portable zip, both macOS payloads and the Linux zip do) → add README or drop it elsewhere; SetupIconFile=installer/autoshade.ico (41,105 B) differs from assets/autoshade.ico (39,434 B) that build.rs embeds → one icon.
- scripts/installer_scenarios.ps1: pre-1.2.4 TrimEnd(';') relaxation dead and `[version]$BaseVersion` throws on a suffixed version; Assert-PayloadInstalled never asserts the ABSENCE of excluded files (test_*.py, *.pyc, weights) → an Excludes regression passes.
- scripts/deploy_site.js: `npx wrangler` unpinned (receives the minted token) → pin; `shell: true` joins args unquoted on Windows (a %TEMP% with a space breaks); a throw from the token DELETE in `finally` masks the deploy error.
- serve-web.cmd: unreferenced duplicate of AutoShade-UI.bat (only the .bat is named anywhere) → delete if unwanted.
- autoshade.local.json is TRACKED although .gitignore says never commit it; its model fields are honoured as an ambient source for anyone running from the checkout, and its base_url fields make the app print the "ignoring the API key and base-URL fields" warning on every run → `git rm --cached` is the owner's call.
- scripts: train_raw.py --resume restores model/opt/sched/iter but not history/best (first post-resume validation always overwrites best_state.pth); grid_experiment.py PROBE names a fitgrid-probe binary that no longer exists; lr_mask_parity.py ALPHA_FLOOR unused while the header claims a floor; noise_synth/diagram_check cosmetic unused constants/params (diagram_check connector label params, denoise_star_standard `acceptance(absolute)` pinned by its test); README.md claims MPS numbers "in the release notes" (now: the release run's macos-battery job log).

## retouch / decode / config / python
- decode.rs TIFF ICC re-probe (F5): the "1 MiB per-value default" is tiff::Limits::ifd_value_size, which tiff 0.11.3 declares but never enforces; the real hole is image 0.25.10 `TiffDecoder::icc_profile` = `Ok(get_tag_u8_vec(IccProfile).ok())`, folding EVERY tiff error into None. Patch (confidence medium; also refuses odd real files that pass as untagged today): probe with `tiff::decoder::Decoder::new(BufReader::new(File::open(path)?))` + match `get_tag_u8_vec(Tag::IccProfile)` { Ok → Some, RequiredTagNotFound → None, other → hard error "read the ICC profile of …" }.
- decode.rs guard_tiff_chain doc (~1632) lists get_decoder callers as "decode_raw, camera_rendition, the full-res sensor render, as_shot_wb" — stale now that decoder_for is the door (frame_size/source_window, raw_orientation, XMP-packet read also guard). Doc drift.
- store.rs migrate_legacy_from_many → migrate_legacy_jobs never consults the explicit-clear tombstone (legacy_suppressed_in) while the per-photo path returns early on it: an explicit "Import legacy" over a cleared develop re-imports. May be intended for an explicit action; low confidence.
- store.rs resume_orphan_adoption_once: a resume beaten by a concurrent one now returns Ok and this probe prints "finished a crashed adoption into …" for work another process did — stderr wording only.
- store.rs B4 variants_member DevelopOnAiPixels arm: active_kind=="generated" with pixels.json missing → no background card minted; active_id/active_name (R24-2 identity that version snapshots point at) are cleared. Candidate fixes: (i) a raster-less `generated` ghost card carrying id/name, (ii) attribute id/name to the fresh `master` card (`id: (outgoing_pristine && recorded.is_none()).then(|| rec.active_id.take()).flatten()`, same for name). Only the pixels.json-present case is pinned. Design call, confidence low-medium.

## render
- render.rs B2 combined_mask_weight: a component's own `inverted` is applied unconditionally; an alpha-less Brush/AiMask/Bitmap component (raster None → weight 0) inverted becomes 1.0 everywhere → Add covers the whole frame at full strength, Subtract wipes the base (Intersect harmless). Options: (a) skip the component when raster-backed and bmp is None (`continue` before the invert, mirrors is_raster_backed); (b) invert only when an alpha exists; (c) refuse the whole adjustment with a disclosure. Lightroom's semantics for an inverted empty brush are arguably "everything" → engine policy. Patch (a) + a test with `MaskComponent{inverted:true, geometry: AiMask, mode: Add}` and `&[None]` asserting weight == base.
- upright.rs B3: the keystone row is built from the UNROTATED vanishing points and applied after the rotate (`rotate.then(warp)`); residual convergence ∝ 1−cos θ (≤0.5 % of the row at 6°). Patch: build the row from `rotate.map_h(pv)` or compose `warp.then(rotate)`. Second-order.
- finish.rs B5: vignette weight sampled at the pixel CORNER (x, y) while CropRect::cx()/cy() are continuous centres → the whole falloff sits ½ px up-left, against the pixel-centre convention render.rs documents on MASK_SAMPLE_CENTRE. Patch: `vg.weight(rect, fx + MASK_SAMPLE_CENTRE, fy + MASK_SAMPLE_CENTRE)` (grain lattice untouched). Every vignetted render's bytes move by a hair near the transition; finish tests assert far from it. Confidence high.
- perspective.rs B7: perspective_scale = 0 → s = 0 → singular map; apply()'s inverse is None → frame returned untouched and every other Transform slider AND the Upright matrix silently dropped with it. recipe.rs documents Scale as 0..=200 and `the_inverse_is_an_inverse_and_a_degenerate_map_has_none` PINS "Scale zero collapses the frame… hand the picture back". Options: (1) floor s at Lightroom's minimum 0.5 + clamp 50..=150 + re-pin the test; (2) floor at ε (1e-3) and re-pin to "a collapsed scale still applies the other sliders"; (3) keep and document on perspective_scale. Design call.
- profile.rs sample() above white: shipped and kept = the chord `lut[n−1]·x` (identity for a curve ending at (1,1)); the alternative is the shoulder's tangent, which compresses every super-white highlight through a .dcp tone curve. Decide on the profile fixtures (a test pins what ships).
- perspective.rs module header (~23–25) claims Adobe's Upright normalisation is "not aspect-aware: a 0.9786° rotation carries… the UNIT SQUARE's cover factor"; the verbatim ADOBE_MATRIX has h10/h01 = −2.25 = −aspect², i.e. it IS the rigid pixel rotation in the per-axis box (true angle 0.6525°, cover 1.01702 ≈ h00/cos θ). Doc-only.
- perspective.rs transform(): `aspect = width/height` while source_of normalises by (w−1, h−1): manual Rotate is rigid only to 1 part in h (sub-0.1 px at the edge). The Upright solver now uses (w−1)/(h−1).

## GUI heal disclosure
- HealReport.skipped now reaches the GUI note, the X-Heal-Skipped header and the web status line; the CLI prints a ⚠ stderr line. A TYPED rationale key (e.g. HEAL_SKIPPED_NO_DONOR with arg n) would make it part of the report's notes instead of a stderr line — needs rationale.rs key + EN sentence + both GUI languages.

## Owner decisions this session did not take

- `autoshade.local.json` is tracked although `.gitignore` names it (see the
  CI/packaging section above).
- `serve-web.cmd` duplicates `AutoShade-UI.bat` and is referenced nowhere.
- The calibration-affecting fit items (fit.rs F1/F3/F6/F7, fit_zoned #2/#3,
  field.rs B2/B3) need `AUTOSHADE_FIT_CALIBRATION_DIR` and the fixtures to
  measure before/after; none was available in this container.
